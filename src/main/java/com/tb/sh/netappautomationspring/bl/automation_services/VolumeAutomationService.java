package com.tb.sh.netappautomationspring.bl.automation_services;

import org.springframework.stereotype.Service;

import java.util.logging.Logger;

@Service
public class VolumeAutomationService {
    private final Logger logger = Logger.getLogger(String.valueOf(VolumeAutomationService.class));
}
